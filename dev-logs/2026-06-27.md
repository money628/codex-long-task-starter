# 开发日志 - 2026-06-27

## 今日完成
- 增加 ProjectSpec 完整度门控：必填信息缺失时禁止生成最终 Markdown 文件包，并提示缺失项。
- ProjectSpec 页“确认生成 Markdown”按钮在信息不完整时禁用并显示“信息未完整”。
- 补齐 CLI npm 发布元数据：描述、关键词、license、engines、files、publishConfig。
- 新增 `apps/cli/README.md`，说明 `init`、`init-files`、`doctor`、`run` 和安全规则。
- 更新需求、技术规范和验收清单。
- 新增 Node 内置测试：覆盖 ProjectSpec 完整度门控、Markdown 密钥泄露检查、JSON 解析和 CLI 写入安全边界。
- 新增 AI 调用层测试：通过本地 OpenAI-compatible 模拟服务验证请求路径、Bearer Key、连接测试和错误响应。
- 新增根脚本 `pnpm test`。
- 修正 DeepSeek 常用端点为 `https://api.deepseek.com`，并在配置页增加 DeepSeek V4 Pro、DeepSeek V4 Flash、GPT-4o 常用模型快捷按钮。
- 增加 ProjectSpec 输入归一化：支持真实模型常见字段别名，如 milestone 的 `title`、`description`、`steps`、`doneWhen`。
- 调整 AI ProjectSpec 生成流程：首轮生成不完整时，自动进行一次必填字段完整度补齐。
- 修复 React `useEffect` 返回 `null` 导致热更新/刷新后空白页的问题。
- 修复访谈页“生成 ProjectSpec”按钮只有样式禁用、没有 HTML `disabled` 属性的问题。
- 结果页 ZIP / JSON 导出增加中文状态反馈，并增强 blob 下载兼容性。
- 补齐 `@codex-starter/core` npm 发布元数据和包内 README，使 CLI 可作为独立包依赖 core 发布。
- 新增 `createExportBundleEntries`，统一 ZIP 导出内容来源，固定包含 7 个 Markdown 文件和 `project-spec.json`。
- 新增发布前验证脚本 `scripts/verify-release.mjs` 和根命令 `pnpm verify:release`。
- 更新开发流程和技术规范，明确 core 必须先于 CLI 发布，并把发布验证列入门禁。

## 验证结果
- 已验证：`pnpm install` 通过。
- 已验证：`pnpm build` 通过。
- 已验证：`pnpm devlog` 通过。
- 已验证：CLI `doctor` 可运行，当前项目根目录缺少 7 个生成文件属于预期提示。
- 已发现并修复：根包与 CLI 包同名会导致 pack 匹配整个工作区，已将根包改名为私有 monorepo 包名。
- 已复验：在 `apps/cli` 目录执行 pack dry-run，只包含 `LICENSE`、`package.json`、`README.md`、`src/index.js`。
- 已复验：`pnpm build` 通过。
- 已复验：CLI `doctor` 可运行，当前项目根目录缺少 7 个生成文件属于预期提示。
- 已静态检查：Web 端存在 ProjectSpec 完整度门控和按钮禁用逻辑。
- 已复验：新增 AI 调用层测试后的 `pnpm test` 通过，8 个测试全部通过。
- 已复验：`pnpm build` 通过。
- 已复验：CLI `doctor` 可运行。
- 已复验：CLI pack dry-run 只包含 CLI 必要文件。
- 已复验：`pnpm devlog` 通过。
- 已复验：`pnpm build` 通过。
- 已复验：`pnpm devlog` 通过。
- 已复验：CLI `doctor` 可运行。
- 已复验：CLI pack dry-run 只包含 CLI 必要文件。
- 已验证：使用用户临时授权的 DeepSeek Key 做最小真实连通性测试，`deepseek-v4-pro`、`deepseek-v4-flash`、`deepseek-chat` 均返回 HTTP 200；日志未记录 Key。
- 已复验：设置 `CI=true` 后 `pnpm install --frozen-lockfile=false`、`pnpm test`、`pnpm build`、`pnpm devlog` 均通过。
- 已验证：Web UI 内使用 DeepSeek Key 通过本地代理完成“测试连接”，页面显示“连接成功”。
- 已验证：真实 AI 动态访谈返回新问题，并通过 InterviewTurnResult JSON 校验后渲染。
- 已发现并修复：真实模型初次 ProjectSpec 生成时 milestone 字段别名导致 Zod 校验失败。
- 已验证：修复后真实 ProjectSpec 可通过 Zod 校验；完整度不足时按钮显示“信息未完整”并禁用。
- 已验证：ProjectSpec 完整度补齐后达到 100%，页面允许生成 Markdown。
- 已验证：真实模式生成 7 个 Markdown 文件，逐个检查未包含 `sk-` 密钥或本次 Key 主体字符串。
- 已验证：ZIP 和 project-spec.json 导出按钮点击后分别显示“已准备 ZIP 下载”和“已准备 project-spec.json 下载”。
- 已复验：新增归一化测试后 `pnpm test` 通过，9 个测试全部通过。
- 已复验：`pnpm build` 通过。
- 已发现并修复：CLI tarball 独立安装会因 `@codex-starter/core` 未发布而失败。
- 已验证：本地打包 `@codex-starter/core` 和 `codex-long-task-starter` 后，在临时项目同时安装 tarball，`npx codex-long-task-starter doctor` 可启动并输出检查表。
- 已验证：`pnpm verify:release` 可自动打包 core 与 CLI、临时安装 tarball，并启动 CLI `doctor`。
- 已复验：导出包条目测试覆盖 7 个 Markdown 文件 + `project-spec.json`，且不包含 API Key。
- 已复验：`pnpm test` 通过，10 个测试全部通过。

## 遗留问题
- 真实供应商 API 已完成 Web UI 端到端短流程验证；仍需用更复杂项目样例观察长对话追问质量和 Markdown 稳定性。
- core 与 CLI 尚未进行真实 npm registry 发布和远程 `npx` 安装验证。
- 浏览器自动化工具未捕获到 blob 下载事件；页面已增加导出状态反馈，仍建议后续在真实浏览器下载目录手动复核一次文件落盘。

## 明日待办
- 用 2-3 个不同项目样例继续检查真实模型多轮访谈质量和 Markdown 输出质量。
- 视需要加入自动化测试覆盖 ProjectSpec 门控和 CLI 写入安全边界。

## 风险
- 未完成真实 Key 端到端验证前，不能宣称所有供应商 API 场景已完全可用。
- CLI 发布前必须确认 tarball 只包含 `apps/cli` 的必要文件，不能包含 workspace 其它目录。
