# 开发日志 - 2026-06-28

## 今日完成
- 创建 2026-06-28 开发日志。
- 新增复杂项目质量回归测试 `tests/quality.test.mjs`。
- 质量回归覆盖 3 类项目：B2B SaaS、Chrome Extension、Developer Tool CLI。
- 测试检查 ProjectSpec 完整度、7 个 Markdown 文件、`project-spec.json` 导出条目、项目关键词相关性、内容长度和 API Key 泄露风险。
- 更新 `docs/acceptance-checklist.md`，记录复杂项目质量回归已覆盖。
- 补齐 v0.1.0-alpha 发布准备：README 快速开始、examples、CONTRIBUTING、GitHub Actions CI、`.gitignore`。
- 模型配置页新增 Provider Presets：OpenAI、DeepSeek、Moonshot / Kimi、通义千问 Qwen、OpenRouter、自定义。
- 优化 Web 关键错误提示：空配置、网络/CORS、401、404/模型名、429、JSON 解析、结构校验失败。
- 包版本统一调整为 `0.1.0-alpha.0`。
- 清理工作区内硬编码 API Key，并移除测试中残留的旧 Key 主体字符串。

## 验证结果
- 已验证：`pnpm devlog` 创建今日日志。
- 已验证：`pnpm test` 通过，11 个测试全部通过。
- 已复验：`pnpm install --frozen-lockfile=false` 通过。
- 已复验：`pnpm build` 通过。
- 已复验：`pnpm verify:release` 通过，core/CLI tarball 可安装且 CLI `doctor` 可启动。
- 已复验：`node apps/cli/src/index.js doctor` 可运行。
- 已验证：Web mock API 全流程跑通：配置、测试连接、创建项目、AI 访谈、ProjectSpec、Markdown、复制、ZIP 状态、JSON 导出状态。
- 已验证：CLI 命令级流程跑通：`init`、`init-files`、`doctor`、`run`。
- 已验证：安全扫描未发现真实 `sk-` Key 或旧 Key 主体字符串。
- 最终复验：`pnpm install --frozen-lockfile=false`、`pnpm test`、`pnpm build`、`pnpm verify:release`、`pnpm devlog`、`node apps/cli/src/index.js doctor` 全部通过。

## 遗留问题
- 真实模型长对话质量仍需用更多真实访谈样例观察。
- core 与 CLI 尚未进行真实 npm registry 发布和远程 registry `npx` 安装验证；v0.1.0-alpha 代码侧已具备发布前验证。

## 明日待办
- 用更复杂的真实业务输入继续检查 DeepSeek 多轮访谈追问深度。
- 视发布计划决定是否执行 npm registry 发布。

## 风险
- 用户此前提供过的 DeepSeek Key 已暴露，正式使用前仍建议作废并更换；当前仓库文件已清理，不再包含该 Key 主体字符串。
- PowerShell 在当前机器上仍有初始化错误，本轮使用 Node 执行命令作为替代。

## 开源发布资料补齐

### 今日完成

- 额外处理安全扫描误报：根私有包名中的 `task-starter` 会被宽松 `sk-` 规则误判，已改为 `clts-monorepo` 并复验通过。
- 新增 `SECURITY.md`，明确 API Key、localStorage、本地代理、浏览器直连和漏洞报告规则。
- 新增 `CHANGELOG.md`，记录 `0.1.0-alpha.0` 的新增能力、安全说明和已知限制。
- 新增 `docs/release-checklist.md`，固化发布前命令、Web 手动流程、CLI 验证和 npm alpha 发布顺序。
- 更新 `README.md`、`codex.md`、`docs/acceptance-checklist.md`，补充发布资料入口。
- 更新 `scripts/verify-release.mjs`，发布验证新增 SECURITY、CHANGELOG、release checklist 文件存在性检查。

### 验证结果
- `pnpm install --frozen-lockfile=false`：通过。
- `pnpm test`：通过，11 项测试通过。
- `pnpm build`：通过，Web 生产构建成功。
- `pnpm verify:release`：通过，core/CLI tarball 临时安装和 CLI doctor 验证成功。
- `pnpm devlog`：通过，当天日志存在。
- `node apps/cli/src/index.js doctor`：通过，按预期提示当前仓库根目录缺少生成文件。

### 遗留问题
- 尚未执行真实 npm registry 发布。
- 远程 registry 的 `npx codex-long-task-starter@alpha doctor` 需发布后验证。

### 明日待办
- 准备 GitHub 仓库描述、topics、首个 Release 草稿。
- 若决定发布 npm alpha，按 `docs/release-checklist.md` 的包顺序执行。

### 风险
- 真实模型长对话质量仍需更多样例持续观察。

## GitHub 开源发布准备

### 今日完成
- 新增 GitHub bug report issue 模板。
- 新增 GitHub feature request issue 模板。
- 新增 PR 模板，要求说明范围、验证命令和安全检查。
- 新增 `docs/github-release.md`，包含仓库名、简介、topics、Release notes 草稿和发布后验证步骤。
- 更新 `README.md`、`codex.md`、`docs/release-checklist.md`、`docs/acceptance-checklist.md`，补充 GitHub 发布资料入口。
- 更新 `scripts/verify-release.mjs`，发布验证新增 GitHub 模板与发布说明检查。

### 验证结果
- `pnpm install --frozen-lockfile=false`：通过。
- `pnpm test`：通过，11 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。
- `node apps/cli/src/index.js doctor`：通过。
- 安全扫描：未发现 `sk-...` 形式的密钥。

### 遗留问题
- 当前机器 Git 根目录被识别为 `C:/`，不适合直接提交；发布前应按 `docs/github-release.md` 建议创建项目独立 Git 仓库。

### 明日待办
- 创建干净 Git 仓库并推送到 GitHub。
- 创建 `v0.1.0-alpha.0` Release。

### 风险
- 不要在系统盘级 Git 仓库中执行全量 add。

## 下一阶段规划文档

### 今日完成
- 新增 `docs/next-planning-brief.md`，用于交给 GPT 或其他协作者讨论下一阶段路线。
- 文档包含项目定位、当前完成度、已实现功能、验证结果、安全状态、风险限制、推荐路线和可直接交给 Codex 的下一步任务。
- 更新 `README.md`、`codex.md`、`docs/acceptance-checklist.md`，补充该规划文档入口。

### 验证结果
- `pnpm test`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。

## GitHub 发布本地仓库准备

### 今日完成
- 在项目目录创建独立 Git 仓库，避免继续使用被识别为 `C:/` 的系统盘级 Git 根。
- 更新 `.gitignore`，排除 `outputs/`、`tech-explain-translator/`、`副业执行系统启动包/` 等非项目目录。
- 只暂存项目相关文件：`apps/`、`packages/`、`docs/`、`examples/`、`scripts/`、`tests/`、`.github/`、README、LICENSE、SECURITY、CHANGELOG、工作指引和 pnpm 配置。
- 创建本地首个提交。
- 创建本地标签 `v0.1.0-alpha.0`。

### 验证结果
- 提交前已通过：`pnpm install --frozen-lockfile=false`。
- 提交前已通过：`pnpm test`，11 项测试通过。
- 提交前已通过：`pnpm build`。
- 提交前已通过：`pnpm verify:release`。
- 提交前已通过：`pnpm devlog`。
- 提交前已通过：`node apps/cli/src/index.js doctor`。
- 提交前安全扫描未发现 `sk-...` 形式密钥。

### 遗留问题
- GitHub CLI 当前未登录，暂时无法自动创建远程仓库、推送或创建 GitHub Release。
- 需要用户完成 GitHub 登录，或提供目标远程仓库 URL。

### 明日待办
- 登录 GitHub CLI 后推送 `main` 和 `v0.1.0-alpha.0` 标签。
- 使用 `docs/github-release.md` 中的 Release notes 创建 GitHub Release。

## GitHub v0.1.0-alpha.0 发布完成

### 今日完成
- 已推送 `main` 到 `https://github.com/money628/codex-long-task-starter`。
- 已推送标签 `v0.1.0-alpha.0`。
- 已在 GitHub 创建 Release：`v0.1.0-alpha.0`。
- Release 已标记为 Pre-release。
- 已确认 GitHub Actions CI 运行完成且结果为 success。

### 验证结果
- GitHub Release API 返回 200。
- Release 地址：`https://github.com/money628/codex-long-task-starter/releases/tag/v0.1.0-alpha.0`。
- CI 地址：`https://github.com/money628/codex-long-task-starter/actions/runs/28317115564`。
- CI 状态：completed / success。

### 遗留问题
- npm alpha 尚未发布。
- 远程 registry 的 `npx codex-long-task-starter@alpha doctor` 尚未验证。

### 明日待办
- 讨论是否进入 npm alpha 发布。
- 若暂不发布 npm，则优先继续打磨 Web 体验和增加示例模板。
