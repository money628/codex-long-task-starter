# 开发日志 - 2026-06-28

## 今日完成
- 创建 2026-06-28 开发日志。
- 新增复杂项目质量回归测试 `tests/quality.test.mjs`。
- 质量回归覆盖 3 类项目：B2B SaaS、Chrome Extension、Developer Tool CLI。
- 测试检查 ProjectSpec 完整度、7 个 Markdown 文件、`project-spec.json` 导出条目、项目关键词相关性、内容长度和 API Key 泄露风险。
- 更新 `docs/acceptance-checklist.md`，记录复杂项目质量回归已覆盖。
- 补齐 v0.1.0-alpha 发布准备：README 快速开始、examples、CONTRIBUTING、GitHub Actions CI、`.gitignore`。
- 模型配置页新增 Provider Presets：OpenAI、DeepSeek、Moonshot / Kimi、通义千问 Qwen、OpenRouter、自定义。
- 优化 Web 关键错误提示：空配置、网络/CORS、401、404/模型名、429、JSON 解析、结构校验失败。
- 包版本统一调整为 `0.1.0-alpha.0`。
- 清理工作区内硬编码 API Key，并移除测试中残留的旧 Key 主体字符串。

## 验证结果
- 已验证：`pnpm devlog` 创建今日日志。
- 已验证：`pnpm test` 通过，11 个测试全部通过。
- 已复验：`pnpm install --frozen-lockfile=false` 通过。
- 已复验：`pnpm build` 通过。
- 已复验：`pnpm verify:release` 通过，core/CLI tarball 可安装且 CLI `doctor` 可启动。
- 已复验：`node apps/cli/src/index.js doctor` 可运行。
- 已验证：Web mock API 全流程跑通：配置、测试连接、创建项目、AI 访谈、ProjectSpec、Markdown、复制、ZIP 状态、JSON 导出状态。
- 已验证：CLI 命令级流程跑通：`init`、`init-files`、`doctor`、`run`。
- 已验证：安全扫描未发现真实 `sk-` Key 或旧 Key 主体字符串。
- 最终复验：`pnpm install --frozen-lockfile=false`、`pnpm test`、`pnpm build`、`pnpm verify:release`、`pnpm devlog`、`node apps/cli/src/index.js doctor` 全部通过。

## 遗留问题
- 真实模型长对话质量仍需用更多真实访谈样例观察。
- core 与 CLI 尚未进行真实 npm registry 发布和远程 registry `npx` 安装验证；v0.1.0-alpha 代码侧已具备发布前验证。

## 明日待办
- 用更复杂的真实业务输入继续检查 DeepSeek 多轮访谈追问深度。
- 视发布计划决定是否执行 npm registry 发布。

## 风险
- 用户此前提供过的 DeepSeek Key 已暴露，正式使用前仍建议作废并更换；当前仓库文件已清理，不再包含该 Key 主体字符串。
- PowerShell 在当前机器上仍有初始化错误，本轮使用 Node 执行命令作为替代。

## 开源发布资料补齐

### 今日完成

- 额外处理安全扫描误报：根私有包名中的 `task-starter` 会被宽松 `sk-` 规则误判，已改为 `clts-monorepo` 并复验通过。
- 新增 `SECURITY.md`，明确 API Key、localStorage、本地代理、浏览器直连和漏洞报告规则。
- 新增 `CHANGELOG.md`，记录 `0.1.0-alpha.0` 的新增能力、安全说明和已知限制。
- 新增 `docs/release-checklist.md`，固化发布前命令、Web 手动流程、CLI 验证和 npm alpha 发布顺序。
- 更新 `README.md`、`codex.md`、`docs/acceptance-checklist.md`，补充发布资料入口。
- 更新 `scripts/verify-release.mjs`，发布验证新增 SECURITY、CHANGELOG、release checklist 文件存在性检查。

### 验证结果
- `pnpm install --frozen-lockfile=false`：通过。
- `pnpm test`：通过，11 项测试通过。
- `pnpm build`：通过，Web 生产构建成功。
- `pnpm verify:release`：通过，core/CLI tarball 临时安装和 CLI doctor 验证成功。
- `pnpm devlog`：通过，当天日志存在。
- `node apps/cli/src/index.js doctor`：通过，按预期提示当前仓库根目录缺少生成文件。

### 遗留问题
- 尚未执行真实 npm registry 发布。
- 远程 registry 的 `npx codex-long-task-starter@alpha doctor` 需发布后验证。

### 明日待办
- 准备 GitHub 仓库描述、topics、首个 Release 草稿。
- 若决定发布 npm alpha，按 `docs/release-checklist.md` 的包顺序执行。

### 风险
- 真实模型长对话质量仍需更多样例持续观察。

## GitHub 开源发布准备

### 今日完成
- 新增 GitHub bug report issue 模板。
- 新增 GitHub feature request issue 模板。
- 新增 PR 模板，要求说明范围、验证命令和安全检查。
- 新增 `docs/github-release.md`，包含仓库名、简介、topics、Release notes 草稿和发布后验证步骤。
- 更新 `README.md`、`codex.md`、`docs/release-checklist.md`、`docs/acceptance-checklist.md`，补充 GitHub 发布资料入口。
- 更新 `scripts/verify-release.mjs`，发布验证新增 GitHub 模板与发布说明检查。

### 验证结果
- `pnpm install --frozen-lockfile=false`：通过。
- `pnpm test`：通过，11 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。
- `node apps/cli/src/index.js doctor`：通过。
- 安全扫描：未发现 `sk-...` 形式的密钥。

### 遗留问题
- 当前机器 Git 根目录被识别为 `C:/`，不适合直接提交；发布前应按 `docs/github-release.md` 建议创建项目独立 Git 仓库。

### 明日待办
- 创建干净 Git 仓库并推送到 GitHub。
- 创建 `v0.1.0-alpha.0` Release。

### 风险
- 不要在系统盘级 Git 仓库中执行全量 add。

## 下一阶段规划文档

### 今日完成
- 新增 `docs/next-planning-brief.md`，用于交给 GPT 或其他协作者讨论下一阶段路线。
- 文档包含项目定位、当前完成度、已实现功能、验证结果、安全状态、风险限制、推荐路线和可直接交给 Codex 的下一步任务。
- 更新 `README.md`、`codex.md`、`docs/acceptance-checklist.md`，补充该规划文档入口。

### 验证结果
- `pnpm test`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。

## GitHub 发布本地仓库准备

### 今日完成
- 在项目目录创建独立 Git 仓库，避免继续使用被识别为 `C:/` 的系统盘级 Git 根。
- 更新 `.gitignore`，排除 `outputs/`、`tech-explain-translator/`、`副业执行系统启动包/` 等非项目目录。
- 只暂存项目相关文件：`apps/`、`packages/`、`docs/`、`examples/`、`scripts/`、`tests/`、`.github/`、README、LICENSE、SECURITY、CHANGELOG、工作指引和 pnpm 配置。
- 创建本地首个提交。
- 创建本地标签 `v0.1.0-alpha.0`。

### 验证结果
- 提交前已通过：`pnpm install --frozen-lockfile=false`。
- 提交前已通过：`pnpm test`，11 项测试通过。
- 提交前已通过：`pnpm build`。
- 提交前已通过：`pnpm verify:release`。
- 提交前已通过：`pnpm devlog`。
- 提交前已通过：`node apps/cli/src/index.js doctor`。
- 提交前安全扫描未发现 `sk-...` 形式密钥。

### 遗留问题
- GitHub CLI 当前未登录，暂时无法自动创建远程仓库、推送或创建 GitHub Release。
- 需要用户完成 GitHub 登录，或提供目标远程仓库 URL。

### 明日待办
- 登录 GitHub CLI 后推送 `main` 和 `v0.1.0-alpha.0` 标签。
- 使用 `docs/github-release.md` 中的 Release notes 创建 GitHub Release。

## GitHub v0.1.0-alpha.0 发布完成

### 今日完成
- 已推送 `main` 到 `https://github.com/money628/codex-long-task-starter`。
- 已推送标签 `v0.1.0-alpha.0`。
- 已在 GitHub 创建 Release：`v0.1.0-alpha.0`。
- Release 已标记为 Pre-release。
- 已确认 GitHub Actions CI 运行完成且结果为 success。

### 验证结果
- GitHub Release API 返回 200。
- Release 地址：`https://github.com/money628/codex-long-task-starter/releases/tag/v0.1.0-alpha.0`。
- CI 地址：`https://github.com/money628/codex-long-task-starter/actions/runs/28317115564`。
- CI 状态：completed / success。

### 遗留问题
- npm alpha 尚未发布。
- 远程 registry 的 `npx codex-long-task-starter@alpha doctor` 尚未验证。

### 明日待办
- 讨论是否进入 npm alpha 发布。
- 若暂不发布 npm，则优先继续打磨 Web 体验和增加示例模板。

## v0.1.0-alpha.0 开源发布文档优化

### 今日完成
- 重写 `README.md` 为英文开源首页，并保留中文入口。
- 新增 `README.zh-CN.md`，面向中文开发者说明项目定位、适合人群、快速开始、功能亮点、安全提醒、已知限制和验证命令。
- 新增 `RELEASE_NOTES.md`，作为 `v0.1.0-alpha.0` 英文正式发布说明。
- 新增 `RELEASE_NOTES.zh-CN.md`，作为中文正式发布说明。
- 强化 `SECURITY.md`，明确 API Key 不应进入导出文件、示例、日志、issue、截图、提交或测试快照，并补充 Key 泄露后的处理步骤。
- 更新 `docs/github-release.md`、`docs/release-checklist.md`，加入中英文 README 和 Release Notes 检查项。
- 更新 `scripts/verify-release.mjs`，发布验证新增 README.zh-CN、Release Notes，并扩展密钥扫描范围到 docs、dev-logs、tests、GitHub 模板和根部发布文档。
- 修正发布验证密钥扫描规则，避免将 `codex-long-task-starter` 中的 `sk-starter` 片段误判为真实 Key。

### 验证结果
- `pnpm install --frozen-lockfile=false`：通过。
- `pnpm test`：通过，11 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。
- `node apps/cli/src/index.js doctor`：通过。
- 安全扫描：未发现 `sk-...` 形式密钥。

### 遗留问题
- GitHub Release 网页内容已发布；若要同步替换为新的完整 Release Notes，需要手动编辑 Release 页面，或在 GitHub CLI 登录后自动更新。
- npm alpha 尚未发布。

### 明日待办
- 决定是否进入 npm alpha 发布。
- 如继续优化 GitHub Release 页面，可将 `RELEASE_NOTES.md` 内容复制到 Release body。

## 中文使用说明收口

### 今日完成
- 将 `README.md` 改为中文默认首页，面向中文开发者说明定位、适合人群、快速开始、Web 流程、API 配置、生成结果、CLI、安全和限制。
- 新增 `README.en.md`，保留英文说明入口。
- 将 `README.zh-CN.md` 调整为中文说明跳转页，避免多份中文 README 内容分叉。
- 新增 `docs/user-guide.zh-CN.md`，提供更完整的中文使用手册，覆盖环境准备、模型配置、创建项目、AI 访谈、ProjectSpec、Markdown、CLI、安全和常见问题。
- 更新 `docs/release-checklist.md` 和 `scripts/verify-release.mjs`，把中文默认 README、英文 README 和中文使用手册纳入发布检查。

### 验证结果
- `pnpm test`：通过，11 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。
- 安全扫描：未发现 `sk-...` 形式密钥。

### 遗留问题
- GitHub Release 页面正文如需中文优先展示，可手动替换为 `RELEASE_NOTES.zh-CN.md` 内容。

## MVP 交互与真实访谈链路修复

### 今日完成
- 修复左侧工作区菜单按钮不可交互的问题：仪表盘、提示词探索、任务规划、终端、历史记录、模型配置现在都会跳转到对应页面。
- 修复侧栏底部按钮：新建项目、设置、支持都有明确动作。
- 修复 ProjectSpec 页面大纲按钮不可交互的问题：点击项目目标、目标用户、MVP 范围、非目标、核心功能、技术约束、风险会切换高亮并滚动到对应区域。
- 修复 AI 访谈页“继续访谈”缺少必填校验反馈的问题：必答项未填会显示中文提示，不再像按钮无效。
- 优化访谈状态文案：真实配置下显示“真实 AI 访谈”，无 Key 时显示“示例模式”。
- 修复结果页“实时编辑 / 纯预览”按钮不可交互的问题：现在可切换编辑器和全宽预览。
- 增强 OpenAI-compatible 调用兼容性：如果供应商不支持 `response_format: json_object`，自动降级重试一次。
- 新增自动化测试覆盖 response_format 不兼容时的降级重试。
- 用本地 mock OpenAI-compatible provider 验证 Web 会通过本地代理真实发起访谈请求，并渲染模型返回问题。

### 验证结果
- `pnpm install --frozen-lockfile=false`：通过。
- `pnpm test`：通过，12 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。
- `node apps/cli/src/index.js doctor`：通过。
- 自动化 Web 冒烟检查通过：主页、创建项目、模型配置、侧栏跳转、访谈必填校验、ProjectSpec、大纲点击、Markdown 结果页、纯预览切换。
- 自动化真实请求链路检查通过：Web 通过本地代理请求 mock `/chat/completions`，页面显示“真实 AI 访谈”并渲染返回问题。
- 安全扫描：未发现 `sk-...` 形式密钥。

### 遗留问题
- 仍需用户用自己的 DeepSeek Key 实测真实外部模型质量和模型名是否正确。
- 如果 DeepSeek 返回模型名不存在或鉴权失败，优先检查 Base URL、Model Name 和 Key。

### 明日待办
- 根据真实试用反馈继续修复 v0.1.1-alpha 的体验问题。
## MVP 审计、清单与安全收口

### 今日完成
- 未找到 `docs/PROJECT_PLAN.md`、`docs/MVP_PLAN.md`、`docs/ROADMAP.md`，本轮基于现有 README、代码结构、docs 与 package scripts 继续推进 MVP。
- 新增 `AGENTS.md`，作为仓库内 Agent 工作入口，明确 local-first、中文体验、安全和验证要求。
- 新增 `docs/MVP_CHECKLIST.md`，把当前 MVP 闭环拆成已完成、待验证和后续打磨项。
- 新增 `examples/README.md`，说明示例项目的用途和文件结构。
- 强化 `packages/core` 的导出安全：ProjectSpec、Markdown、ZIP bundle 入口都会对疑似 API Key 做脱敏。
- 更新 `tests/core.test.mjs`，增加 API Key 脱敏回归测试。
- 更新 `scripts/verify-release.mjs`，把 `AGENTS.md`、`docs/MVP_CHECKLIST.md`、`examples/README.md` 纳入发布前检查。

### 验证结果
- `pnpm install --frozen-lockfile=false`：通过。
- `pnpm test`：通过，13 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。
- `node apps/cli/src/index.js doctor`：通过；在仓库根目录运行时会按预期提示部分生成文件不存在。
- 安全扫描：未发现真实 `sk-...` 形式 API Key 写入仓库文件。

### 遗留问题
- 用户在聊天中再次暴露真实 DeepSeek API Key，应立即撤销并重新创建；该 Key 未写入仓库文件。
- 真实 DeepSeek 外部模型的完整多轮访谈质量仍需用新 Key 在 Web 中人工复测。
- npm alpha 发布尚未执行。
- 桌面快捷方式仍是本地辅助启动脚本，不是正式 Windows 安装器。

### 明日待办
- 用新 DeepSeek Key 重新跑通 Web 全流程：配置、测试连接、AI 访谈、ProjectSpec、Markdown、ZIP、JSON 导出。
- 继续优化 README 中文使用说明和常见问题。
- 若确认进入 npm alpha，执行 npm 发布前验证与发布。

### 风险
- 不要再在聊天、截图、日志、issue 或提交中暴露真实 API Key。
- 旧 Key 已视为泄露，继续使用会有账户安全风险。

## 中文试用文档与 CLI 命令校准

### 今日完成
- 更新 `README.md`：补充没有 pnpm 时的安装方式，避免 Windows 新用户启动时卡在“未找到 pnpm”。
- 更新 `README.md`：将 CLI 说明从未发布 npm 的 `npx` 命令改为当前仓库可直接执行的本地源码命令。
- 更新 `docs/user-guide.zh-CN.md`：补充 pnpm 安装提示、本地 CLI 命令和 npm alpha 发布后的命令区别。

### 验证结果
- `pnpm test`：通过，13 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。
- `node apps/cli/src/index.js doctor`：通过；仓库根目录缺少生成任务包文件的提示符合预期。

### 遗留问题
- npm alpha 仍未发布，因此文档中只保留 `npx codex-long-task-starter@alpha doctor` 作为发布后的示例。

### 明日待办
- 继续用真实项目检查 README 是否足够让新用户 5 分钟跑通。

### 风险
- 文档中的命令必须和当前仓库真实可执行状态保持一致，避免用户复制后失败。

## 发布校验与中文入口页增强

### 今日完成
- 更新 `scripts/verify-release.mjs`：新增 CLI 文档一致性检查，确保 npm alpha 未发布前 README 和中文手册必须包含本地 CLI 命令。
- 更新 `scripts/verify-release.mjs`：禁止文档误写未发布状态下的普通 `npx codex-long-task-starter` 命令，只允许 `@alpha` 发布后示例。
- 重写 `README.zh-CN.md`：从简单跳转页改为中文入口页，包含项目定位、快速启动、当前 CLI 用法和安全提醒。

### 验证结果
- `pnpm test`：通过，13 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过，新增 CLI 文档一致性检查通过。
- `pnpm devlog`：通过。
- `node apps/cli/src/index.js doctor`：通过；仓库根目录缺少生成任务包文件的提示符合预期。

### 遗留问题
- npm alpha 未发布前，CLI 文档仍以本地源码命令为准。

### 明日待办
- 继续检查 Web 页面真实交互和 DeepSeek 新 Key 全流程。

### 风险
- 如果未来发布 npm alpha，需要同步更新 README 中的 CLI 命令和发布校验规则。

## Web 结果页导出与 CLI 命令修复

### 今日完成
- 修复 Web 结果页 CLI 命令：从未发布 npm 的 `npx codex-long-task-starter` 改为当前仓库可执行的 `node apps/cli/src/index.js init --spec ./project-spec.json`。
- 修复 Web `project-spec.json` 导出路径：改用 `createExportBundleEntries` 生成 JSON，和 ZIP 共享同一条脱敏入口。
- 增强复制失败提示：剪贴板权限不可用时给出中文提示，并提供旧浏览器 fallback。
- 增强 ZIP / JSON 导出失败提示，避免按钮无反应。
- 更新 `scripts/verify-release.mjs`：发布前检查 README、中文手册、中文入口页和 Web 源码中不能出现未发布 npm 的普通 `npx codex-long-task-starter` 命令。
- 更新 `docs/MVP_CHECKLIST.md`：记录本轮真实试用问题已修复。

### 验证结果
- `pnpm install --frozen-lockfile=false`：通过。
- `pnpm test`：通过，13 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过，新增 Web 源码 npx 命令检查通过。
- `pnpm devlog`：通过。
- `node apps/cli/src/index.js doctor`：通过；仓库根目录缺少生成任务包文件的提示符合预期。

### 遗留问题
- npm alpha 未发布前，Web 和文档都继续使用本地源码 CLI 命令。

### 明日待办
- 继续用浏览器手动检查结果页复制、ZIP 下载和 JSON 导出。

### 风险
- JSON 导出和 ZIP 导出必须始终共享脱敏逻辑，避免未来出现两条不一致的导出路径。

## API 配置空格容错

### 今日完成
- 更新 `packages/ai/src/index.js`：统一清理 Base URL、API Key、Model Name 前后空格。
- 更新 `packages/ai/src/index.js`：纯空白 Base URL / API Key / Model Name 不再被视为可用配置。
- 更新 `tests/ai.test.mjs`：新增配置 trim 和空白配置不可用的回归测试。
- 更新 `docs/MVP_CHECKLIST.md`：记录复制粘贴空格导致连接失败的问题已修复。

### 验证结果
- `pnpm test`：通过，14 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。
- `node apps/cli/src/index.js doctor`：通过；仓库根目录缺少生成任务包文件的提示符合预期。

### 遗留问题
- 真实 DeepSeek Key 仍需由用户重新创建后在 Web 中人工复测。

### 明日待办
- 继续检查模型配置页是否需要更明显提示“复制 Key 后请不要带空格”。

### 风险
- 不要在测试、日志或文档中写入真实 API Key；测试继续使用短占位 Key。

## CLI init-files 空目录提示

### 今日完成
- 更新 `apps/cli/src/index.js`：新增 `readMarkdownFilesFromDir`，读取已生成 Markdown 目录时如果没有找到任何任务文件，会抛出明确中文错误。
- 更新 `apps/cli/src/index.js`：`init-files` 复用该读取函数，避免空目录情况下显示“已处理”但实际没有写入。
- 更新 `tests/cli.test.mjs`：新增空目录回归测试。
- 更新 `docs/MVP_CHECKLIST.md`：记录 CLI `init-files` 空目录误导问题已修复。

### 验证结果
- `pnpm test`：通过，15 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。
- `node apps/cli/src/index.js doctor`：通过；仓库根目录缺少生成任务包文件的提示符合预期。

### 遗留问题
- CLI 命令行错误目前仍由 Node/Commander 输出，后续可以继续做更友好的顶层错误格式。

### 明日待办
- 继续用临时目录验证 `init --spec` 和 `init-files` 的真实写入体验。

### 风险
- CLI 只能写入当前目录内文件，后续不要为了方便破坏路径安全限制。

## CLI 友好错误输出

### 今日完成
- 更新 `apps/cli/src/index.js`：新增 `runCli`，统一捕获命令执行异常。
- CLI 失败时现在输出 `错误：...` 并设置非 0 退出码，避免默认暴露 Node 堆栈。
- 更新 `tests/cli.test.mjs`：新增子进程测试，验证 `init-files` 空目录失败时有中文错误且不输出堆栈。
- 更新 `docs/MVP_CHECKLIST.md`：记录 CLI 友好错误输出已完成。

### 验证结果
- `pnpm test`：通过，16 项测试通过。
- `pnpm build`：通过。
- `pnpm verify:release`：通过。
- `pnpm devlog`：通过。
- `node apps/cli/src/index.js doctor`：通过；仓库根目录缺少生成任务包文件的提示符合预期。

### 遗留问题
- 后续可以继续把不同错误类型映射成更具体的修复建议，例如 spec 文件不存在、JSON 不合法、schema 校验失败。

### 明日待办
- 用临时目录继续手动试跑 `init --spec` 与 `init-files`。

### 风险
- CLI 仍应保持简单，不引入复杂交互框架。
