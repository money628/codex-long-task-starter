# @codex-starter/core

Shared core utilities for Codex Long Task Starter.

This package contains:

- Zod schemas for `InterviewTurnResult` and `ProjectSpec`
- ProjectSpec validation and completeness checks
- JSON parsing helpers for model responses
- Markdown file generation from a validated ProjectSpec

It is used by the Web app, AI package, and CLI package.
